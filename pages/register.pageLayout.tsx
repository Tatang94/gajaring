
import React from "react";

interface RegisterPageLayoutProps {
  children: React.ReactNode;
}

export function RegisterPageLayout({ children }: RegisterPageLayoutProps) {
  return <>{children}</>;
}
